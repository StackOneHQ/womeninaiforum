# Women in AI Forum

Community website hosted on Cloudflare Pages.

## Dev Setup

```sh
npm install
npm run dev
```

Dev server runs at `localhost:4321`.

## Build

```sh
npm run build
```

## Deploy

Push to `master` deploys to production automatically via Cloudflare Pages.
Push to any other branch creates a preview deployment.
